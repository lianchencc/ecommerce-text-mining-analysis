# Data

The original course-project datasets are not included in this public repository.

Expected files:

- `merged_df.csv`
- `TRBOX_comment_df.csv`
- `monthly/商品資料-*.xlsx`

Place the files locally according to the paths above before running the notebook.
